{
  "name": "portfolio-os",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev:gui": "vite packages/gui",
    "test": "vitest",
    "build": "tsc -b && vite build",
    "deploy": "vercel deploy",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@biomejs/biome": "1.8.3",
    "@playwright/test": "^1.48.2"
  }
}
